# Domain/subdomain pointing to this server
DOMAIN=live.example.com

# Long private secret required by /api/start and /api/stop
RELAY_SECRET=change-this-long-random-secret

# Public IPv4 of the server; SRS uses it as WebRTC ICE candidate
PUBLIC_IP=203.0.113.10
